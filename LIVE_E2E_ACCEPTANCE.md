# LunaTVSource v0.4.62 实机端到端验收记录

## 结论

- 验收日期：2026-09-01（Asia/Shanghai）
- 实机环境：MoviePilot `http://192.168.66.66:33000`
- 被测版本：LunaTVSource `v0.4.62`
- 仓库基线：`7063d8b6d88bef9083fcfc2c7f845952a37009ec`
- 发布标签：`LunaTVSource_v0.4.62`
- 最终结果：**失败，未通过端到端验收**
- 阻塞点：MoviePilot 原生下载入口收到提交，但未返回任务 ID，也未将任务写入原生下载管理或 LunaTVSource 队列。

本次仅执行验证和记录。未修改产品代码、插件配置、凭据、权限、下载历史或测试文件，也未清理任何已有数据。

## 验收范围

目标链路：

1. MoviePilot 原生搜索发现 LunaTVSource 资源。
2. 从原生资源结果发起一次真实下载。
3. LunaTVSource 持久化队列接收任务。
4. N_m3u8DL-RE 完成 HLS 下载。
5. MoviePilot 完成原生整理。
6. 媒体库同步后在 Emby/Jellyfin 可见。

本次测试内容为公开测试电影：

- 标题：`Sintel / 寻龙记`
- 年份：`2010`
- TMDB ID：`45745`
- 媒体类型：电影

由于测试内容是电影，“电视剧订阅进度递增”不适用，结果记为 N/A。

## 实机前置状态

在 MoviePilot 插件页面确认：

- LunaTVSource 版本为 `v0.4.62`。
- N_m3u8DL-RE 显示“已就绪”。
- 下载目录为 `/media/incoming`。
- 并发配置为 `2` 个任务、每任务 `16` 个分片。
- 媒体库自动刷新已启用。
- 当前资源清单包含 `72` 个来源。
- 队列初始状态为：运行 `0`、等待 `0`、暂停 `0`、失败 `0`、总计 `237`。

插件页面同时显示：远程来源清单刷新失败，当前使用本地缓存，错误为 `The read operation timed out`。该告警未阻止本次原生搜索；被选中的“最大资源”在来源健康表中显示为已启用、正常并支持搜索，因此不能仅据此把远程清单超时认定为下载提交失败的根因。

## 执行记录

### 第一次提交

1. 原生搜索返回 `9` 个 LunaTVSource 资源。
2. 选择“最大资源”的 `576P` 结果。
3. 点击“开始下载”一次。
4. 下载弹窗关闭，但原生下载管理仍显示“没有任务”。
5. LunaTVSource 队列仍为运行 `0`、等待 `0`、失败 `0`、总计 `237`。

第一次提交没有取得请求层证据，因此没有重复盲点提交；随后启用网络取证，执行第二次、也是最后一次提交。

### 第二次提交与网络证据

搜索阶段：

- `2026-09-01 12:35:07`：MoviePilot 建立 TMDB `45745` 的渐进式搜索流。
- `2026-09-01 12:37:16`：搜索完成，共 `8` 个资源。
- 日志确认“最大资源 · 769ms”匹配到 `寻龙记 (2010) · 576P`。

提交阶段：

- 提交时间：`2026-09-01 12:41:59`。
- 用户操作：选择“最大资源 · 769ms”的 `576P` 结果，点击“开始下载”一次。
- 请求：`POST /api/v1/download/`。
- HTTP 状态：`200`。
- 响应体长度：`48` 字节。
- 业务响应：`success=false`，`message=任务添加失败`。
- 未返回任务 ID。

请求的非敏感字段摘要：

- `media_source=themoviedb`
- `media_id=45745`
- 顶层 `downloader=null`
- 顶层 `save_path=null`
- 资源内 `download_path=/media/incoming`
- 资源内 `site_name=🎬最大资源 · 769ms`
- 资源分类为电影

`downloader=null` 和 `save_path=null` 是实际请求中的观察值，但当前证据不足以证明它们就是失败原因；资源对象同时携带了 `/media/incoming`，且插件桥接代码会按插件目录、MoviePilot 传入目录和媒体类型目录依次解析下载根目录。

为避免泄露凭据，本记录不包含请求头、Cookie、Bearer、资源令牌、完整请求体或完整播放地址。

## 提交后的状态核验

| 检查点 | 结果 | 实际证据 |
| --- | --- | --- |
| 插件版本和运行前置 | 通过 | v0.4.62；N_m3u8DL-RE 已就绪；目录与并发配置可见 |
| MoviePilot 原生搜索 | 通过 | 第二次搜索返回 8 个资源；日志确认 576P 结果完成标题匹配 |
| 原生下载 API 接收请求 | 通过 | `POST /api/v1/download/` 返回 HTTP 200 |
| API 业务入队 | **失败** | `success=false`、`message=任务添加失败`、无任务 ID |
| MoviePilot 原生下载管理出现任务 | **失败** | 页面仍显示“没有任务” |
| LunaTVSource 队列出现任务 | **失败** | 运行 0、等待 0、失败 0、总计仍为 237 |
| 首页下载速度和已下载量 | 未执行 | 没有任务进入运行态 |
| HLS 扫描统计（CUE/正则/DATERANGE/DISCONTINUITY） | 未执行 | 下载器未启动 |
| N_m3u8DL-RE 下载完成 | 未执行 | 下载器未启动 |
| MoviePilot 原生整理 | 未执行 | 没有完成文件 |
| Emby/Jellyfin 媒体库可见 | 未执行 | 没有可同步的完成媒体 |
| 电视剧订阅进度递增 | N/A | 测试内容为电影 |

## 失败边界与代码映射

仓库中的 LunaTVSource 原生下载桥接位于 `plugins.v3/lunatvsource/__init__.py` 的 `download()` 方法（约第 6294–6390 行）：

- 无法解析 LunaTV 资源令牌时返回 `None`，由宿主继续处理。
- 队列不存在或无法解析下载根目录时返回 `LunaTV 下载参数无效`。
- 资源 URL、季或集参数无效时不会入队；全部无效时返回 `LunaTV 下载参数无效`。
- 任务已在队列或历史记录中时返回 `任务已在下载队列或历史记录中`。
- 成功时必须由 `queue.enqueue(task)` 产生任务 ID，并返回下载器名 `LunaTVSource`。

实机响应只暴露了宿主层通用信息 `任务添加失败`，最近实时日志中也没有与 `12:41:59` 提交对应的更具体异常。因此目前只能确认：**MoviePilot 原生下载入口没有得到可用任务 ID，LunaTVSource 队列没有接收新任务**。

现有证据不能在以下情况中做唯一判定：

1. 宿主没有成功调用或接受 LunaTVSource 下载桥接结果。
2. 宿主调用桥接后把插件返回的“参数无效”或“历史重复”信息折叠成了通用失败信息。
3. 请求字段与当前 MoviePilot 版本的下载桥接契约不一致。

## 仓库级验证证据

在相同 Git 基线、工作树未变化时，已完成：

- `python3 -m pytest tests`：`490 passed`
- Python 编译检查：通过

这些结果证明仓库内测试基线通过，但不能替代本次失败的实机下载、整理和媒体库链路。

## 最终判定与后续入口

LunaTVSource `v0.4.62` 本次实机端到端验收 **不通过**。搜索链路通过，失败发生在原生下载提交到插件队列之间；下载、HLS 扫描、完成、整理和媒体库同步均未到达，不能声明通过。

下一轮复验前应先让 MoviePilot 下载入口暴露或记录 LunaTVSource 桥接的实际返回值/异常，并确认该请求为什么没有产生任务 ID。完成该单点定位或修复后，使用同一公开电影重新执行完整链路即可；本记录不授权或实施修复。
